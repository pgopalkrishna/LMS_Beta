@model IEnumerable<LMSService.ViewModels.vwOrganization>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create"><strong><i class="fas fa-plus"></i></i></strong> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrgnizationName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrgAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrgnizationName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrgAddress)
            </td>
            <td>
                @Html.ActionLink(" Edit", "Edit", new { id=item.Id },new { @class= "fas fa-pen-square" }) |
                @*@Html.ActionLink("Details", "Details", new {id=item.Id }) |*@
                @Html.ActionLink(" Delete", "Delete", new { id=item.Id  },new { @class= "far fa-trash-alt" })
            </td>
        </tr>
}
    </tbody>
</table>
